//#include <iostream>
//#include <iomanip>
//int main() {
//    float a, b, c, s, p;
//    std::cin >> a >> b >> c;
//    p = 0.5 * (a + b + c);
//    s = sqrt(p * (p - a) * (p - b) * (p - c));
//    std::cout << std::fixed << std::setprecision(1) << s;
//}